# ===================================
# Automagik Tools Hub - Zero Configuration
# ===================================
#
# The Hub uses a database-first configuration approach.
# After running the setup wizard at /setup, all configuration
# is stored in the encrypted SQLite database.
#
# This .env file is ONLY needed for bootstrap (before database exists).
# All other configuration is managed through:
#   - Setup Wizard: /setup (WorkOS credentials, admin emails, network config)
#   - Tool Catalogue: /catalogue (per-tool OAuth, API keys, parameters)
#
# See docs/CONFIGURATION.md for complete configuration guide.

# Database Configuration (Bootstrap Only)
# The Hub supports both SQLite (default) and PostgreSQL (production)
# Choose one option below:

# Option 1: SQLite (default, zero configuration)
HUB_DATABASE_PATH=./data/hub.db

# Option 2: PostgreSQL (production)
# Uncomment and configure the line below to use PostgreSQL:
# HUB_DATABASE_URL=postgresql://user:password@localhost:5432/automagik_hub

# ===================================
# That's it! 🎉
# ===================================
# Visit http://localhost:8000/setup to configure your Hub
