# ===================================
# Publishing Configuration
# ===================================
# PYPI_USERNAME=namastexlabs
# PYPI_TOKEN=pypi-xxx...

# ===================================
# Hub Multi-Tenant Server
# ===================================

# Server Configuration
HUB_HOST=0.0.0.0
HUB_PORT=8884
HUB_BASE_URL=http://localhost:8884

# Database
HUB_DATABASE_PATH=./hub_data/hub.db
SQL_ECHO=false

# WorkOS AuthKit (User Authentication)
# Get your AuthKit domain from: https://dashboard.workos.com/
# Navigate to: Authentication → AuthKit → Configuration
# Example: https://client-abc123.authkit.app
WORKOS_AUTHKIT_DOMAIN=https://your-authkit-domain.authkit.app

# ===================================
# Google OAuth (For Google Tools)
# ===================================
# These credentials are for Google Workspace tools (Calendar, Gmail, Drive, etc.)
# NOT for Hub user authentication (that uses WorkOS above)
# Get from: https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8884/auth/google/callback
GOOGLE_OAUTH_AUTO_OPEN_BROWSER=true

# ===================================
# Multi-Tenant Ready Tools
# ===================================
# These tools support per-user credentials via Hub database
# ✅ Google Workspace Tools (gmail, calendar, drive, docs, sheets, slides, forms, tasks, chat)

# --- Google Workspace (Individual Tools) ---
# Uses shared OAuth credentials above
# Per-user tokens stored in Hub database
# Optional overrides:
# GOOGLE_WORKSPACE_CREDENTIALS_DIR=~/.google_workspace_mcp/credentials
# GOOGLE_WORKSPACE_TOOL_TIER=core
# GOOGLE_WORKSPACE_USER_EMAIL=your-email@example.com
# GOOGLE_WORKSPACE_ENABLE_OAUTH21=false
# GOOGLE_WORKSPACE_SINGLE_USER_MODE=true
# GOOGLE_WORKSPACE_STATELESS_MODE=false
# GOOGLE_WORKSPACE_BASE_URI=http://localhost
# GOOGLE_WORKSPACE_PORT=8000
# GOOGLE_WORKSPACE_LOG_LEVEL=INFO

# ===================================
# Single-Tenant Tools (NOT Hub Compatible)
# ===================================
# ⚠️ WARNING: These tools are NOT adapted for multi-tenant Hub
# They use global configuration and cannot isolate per-user credentials
# Use these tools in standalone mode only, NOT via Hub
# See docs/MULTI_TENANT_STATUS.md for migration status

# --- Evolution API (WhatsApp Business) ❌ NOT MULTI-TENANT ---
# Status: Global client, no user isolation
# Use standalone only - NOT via Hub
# EVOLUTION_API_BASE_URL=https://api.evolution.com
# EVOLUTION_API_API_KEY=your-api-key
# EVOLUTION_API_INSTANCE=your-instance-name
# EVOLUTION_API_FIXED_RECIPIENT=optional-fixed-number
# EVOLUTION_API_TIMEOUT=30
# EVOLUTION_API_MAX_RETRIES=3

# --- OMNI (Multi-Tenant WhatsApp) ❌ NOT MULTI-TENANT ---
# Status: Global client, no user isolation
# Use standalone only - NOT via Hub
# OMNI_BASE_URL=http://localhost:8882
# OMNI_API_KEY=your-api-key
# OMNI_DEFAULT_INSTANCE=genie
# OMNI_TIMEOUT=30
# OMNI_MAX_RETRIES=3
# OMNI_MASTER_PHONE=your_phone_number
# OMNI_MASTER_GROUP=your_group_id@g.us

# --- Genie OMNI (Agent-First WhatsApp) ❌ NOT MULTI-TENANT ---
# Status: Wrapper around OMNI, inherits same limitation
# Uses OMNI_* environment variables (same as above)
# Use standalone only - NOT via Hub

# --- Gemini Assistant ❌ NOT MULTI-TENANT ---
# Status: Global session storage, no user isolation
# Use standalone only - NOT via Hub
# GEMINI_API_KEY=your-google-gemini-api-key
# GEMINI_MODEL=gemini-2.0-flash-exp
# GEMINI_SESSION_TIMEOUT=3600
# GEMINI_MAX_TOKENS=8192
# GEMINI_TEMPERATURE=0.7
# GEMINI_MAX_SESSIONS=10

# --- Spark (Workflow Orchestration) ❌ NOT MULTI-TENANT ---
# Status: Global API client, no user isolation
# Use standalone only - NOT via Hub
# SPARK_BASE_URL=http://localhost:8883
# SPARK_API_KEY=namastex888
# SPARK_TIMEOUT=30
# SPARK_MAX_RETRIES=3

# --- AutoMagik Hive ❌ NOT MULTI-TENANT ---
# Status: Global API client, no user isolation
# Use standalone only - NOT via Hub
# HIVE_API_BASE_URL=http://localhost:8886
# HIVE_API_KEY=your-api-key
# HIVE_TIMEOUT=30

# --- Genie Tool (MCP Orchestrator) ❌ NOT MULTI-TENANT ---
# Status: Global OpenAI client and memory, no user isolation
# Use standalone only - NOT via Hub
# OPENAI_API_KEY=sk-your-openai-api-key
# GENIE_MODEL=gpt-4.1
# GENIE_MEMORY_DB=genie_memory.db
# GENIE_STORAGE_DB=genie_storage.db
# GENIE_SESSION_ID=global_genie_session
# GENIE_HISTORY_RUNS=3
# GENIE_SHOW_TOOL_CALLS=true
# GENIE_MCP_CLEANUP_TIMEOUT=2.0
# GENIE_SSE_CLEANUP_DELAY=0.2
# GENIE_AGGRESSIVE_CLEANUP=true

# ===================================
# Utility Tools (No Authentication)
# ===================================

# --- Wait Utility ✅ COMPATIBLE ---
# No authentication needed, works with Hub
# WAIT_MAX_DURATION=3600
# WAIT_DEFAULT_PROGRESS_INTERVAL=1.0

# ===================================
# Service Account Tools (Different Auth Model)
# ===================================

# --- JSON to Google Docs ⚠️ SERVICE ACCOUNT ---
# Uses Google service account (not user OAuth)
# Service account is already per-user isolated
# May work with Hub depending on use case
# GOOGLE_DOCS_SERVICE_ACCOUNT_JSON=path/to/service-account.json
# GOOGLE_DOCS_SERVICE_ACCOUNT_JSON_CONTENT={"type":"service_account",...}
# GOOGLE_DOCS_DEFAULT_FOLDER_ID=google-drive-folder-id
# GOOGLE_DOCS_ENABLE_MARKDOWN_CONVERSION=true
# GOOGLE_DOCS_DEFAULT_SHARE_TYPE=reader
# GOOGLE_DOCS_TIMEOUT=300

# ===================================
# External MCP Servers
# ===================================
# Mount external MCP servers (stdio or HTTP) into the Hub
# Servers will be auto-discovered and mounted with their own prefixes
#
# Format: JSON object with server name as key
# Each server needs:
#   - type: "stdio" or "http"
#   - For stdio: command, args (optional), env (optional)
#   - For HTTP: url
#
# Example stdio server (filesystem):
# MCP_SERVERS_CONFIG='{"filesystem":{"type":"stdio","command":"npx","args":["-y","@modelcontextprotocol/server-filesystem","/tmp"]}}'
#
# Example HTTP server:
# MCP_SERVERS_CONFIG='{"remote_api":{"type":"http","url":"http://localhost:9000/mcp"}}'
#
# Multiple servers example:
# MCP_SERVERS_CONFIG='{"filesystem":{"type":"stdio","command":"npx","args":["-y","@modelcontextprotocol/server-filesystem","/tmp"]},"weather":{"type":"http","url":"http://localhost:9001/mcp"}}'

# ===================================
# Optional External Services
# ===================================

# ElevenLabs Text-to-Speech (for talk/voice tools)
# XI_API_KEY=sk_your_api_key
# XI_VOICE_ID=your_voice_id
